import React from "react"
import { PageProps, Link, graphql } from "gatsby"

import { getPrice } from "../dal/price"

export default function Price(props: PageProps) {
  console.log("props", props)

  return <div>FUCK</div>
}
